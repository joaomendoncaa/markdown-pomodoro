/**
 * Handle saving recoding the pomodoro metadata on a markdown file
 */
export async function recordMetadata({
	path,
	tasks,
}: {
	path: string;
	tasks: Tasks;
}): Promise<boolean> {
	// TODO: for every task, record them on the file metadata

	return false;
}
